# Copy to .env for local development:
#   cp .env.example .env

# Where the pickled sklearn artifacts live. Relative to the working directory.
# Drop bike_availability_model.pkl and model_features.pkl here before starting.
MODEL_DIR=machine_learning

# Override the filenames if your training pipeline emits different names.
# MODEL_FILENAME=bike_availability_model.pkl
# FEATURES_FILENAME=model_features.pkl
